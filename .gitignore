node_modules/
dist/
.env
*.log
*.bak
server/logs/
data/*.sqlite*
*.vsidx
CopilotIndices/
.vs/
server/logs-alt/
data/raw/
data/archive-alt/
# D169: archived tip-sheet screenshots and their model responses. Not committed
# for the same reason scratch/tip-samples/ is not - they are screenshots of
# someone else's app, and they are per-day personal captures rather than the
# one shared fixture data/archive/equibase-otr/ deliberately carries.
data/archive/tip-picks/
dist-static/
scratch/
