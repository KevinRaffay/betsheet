# Feature status - notes as they stood before the 2026-09-04 trim

CLAUDE.md is loaded in full every session and has a hard size limit. On
2026-09-04 it went over that limit at 165k characters, and its Feature status
rows - which had grown into second copies of their ledger entries - were
compressed to a paragraph each.

This file is the verbatim snapshot of those rows, kept so the trim destroys
nothing. It is a frozen record: **do not update it.** A row here may describe
behaviour that has since changed.

- The ledger of record is `DELIVERABLES.md`, one row per deliverable ID.
- The current state of a feature is CLAUDE.md's own Feature status table.
- This file only answers "what did CLAUDE.md say about D<N> before the trim?"

78 rows, verbatim.

| feature | state | notes |
| --- | --- | --- |
| Scratch environment: betsheet-alt is a separate clone (D83) | in review | PR [#118](https://github.com/KevinRaffay/betsheet/pull/118), branch `alt-environment-docs` (the first D83 commit reached main inside PR #117, whose branch was cut from this one; #118 carries the follow-up below) - a factory reset touches exactly two paths (`BETSHEET_DB` and `BETSHEET_LOG_DIR`), so the old alt setup - this checkout with those redirected - was safe only if every override was remembered. Now a separate clone at `C:\repos\betsheet-alt` where the defaults resolve inside it, with only ports overridden (8798/5178, outside the 8899-8920 check-script band the old 8902 sat inside) and `BETSHEET_RAW_DIR` sharing this repo's archive, which a reset never touches. README gains a prominent "The scratch environment" section; the "Two instances, two log dirs" gotcha is rewritten. Proven by resetting the clone and confirming main still had 74 race days / 96 cards, byte-identical files, 73 archived days. Follow-up on the same branch: the README's by-hand launch line was cmd.exe `set X=Y&&` inside a bash fence, which exports nothing in Git Bash - the ports stayed default and the preflight blamed the main app. The ports now come from the clone's own (gitignored) `.env`, with `import 'dotenv/config'` added to `scripts/dev-preflight.js`, `scripts/dev-watch.js`, `scripts/dev-clean.js` and `vite.config.js` so the whole stack moves together instead of only the server; `npm run dev` is the entire command in both checkouts. Verified: api 8798 + vite 5178 up with no prefix (in Git Bash AND PowerShell), `/api/race-days` 200 direct and through the proxy, main's 8788 untouched. Second follow-up, found while answering "will it never conflict?": `npm run dev:clean -- --yes` in MAIN would have stopped the scratch clone, because it sweeps vite's fallback range (5176-5180) and alt's vite is 5178 - a stack that was never in main's way, since `strictPort` means main only ever wants 5175. `scripts/dev-clean.js` now sweeps a fallback port only when the holding process tree is provably this checkout's. Proven live with both stacks up: the discriminator separates them on all four ports, a forced overlap (`BETSHEET_VITE_PORT=5174`) reports main's 5175 as "in use by another checkout, left alone", and `--yes` from alt stopped only alt while main kept serving |
| Sports from the Basement removed as a source (D82) | merged | PR [#115](https://github.com/KevinRaffay/betsheet/pull/115), branch `remove-sftb` - user decision 2026-09-04: SFTB will not be used as a source of anything. A `git revert` was considered first and rejected on evidence, not taste: the fetcher landed 278 commits back (`adb8e8b`, D08c), and its only other commit (`2edd6f2`, D53) also carries shipped migration 013 plus generic consensus-framework work (the `not_published` outcome, the fetch-audit discovery columns, ConsensusPanel's URL/Discovery columns) - reverting it would delete a shipped migration, which this file's own gotcha forbids, and would take four unrelated features with it. Removed instead: `server/fetchers/sftb.js` and its registration, the page/sitemap fixtures it parsed, and `scripts/check-sources.js` (which existed only for SFTB - every other source has its own check script). The one generic test inside it, `not_published` never counting toward the backoff, moved to `check-consensus.js` where the rule actually lives. `pickerAgreement` was pinned to SFTB by name (`sftbTopPgm`/`matchesSftbTop`) and is now the top pick from whichever EXTERNAL source the race has (`s.kind != 'program'`, lowest source id for stability), so the Replay standing keeps working on ATR/OTR days. Stored data: `npm run remove-sftb-picks -- --yes` deleted the 24 `consensus_picks` rows, re-classified the one affected day through the same `classifyAndPersist` the fetch runner uses, and disabled the source row; the 4 `fetch_attempts` rows and every decision-trace entry are KEPT, because invariant 11 exists so a source's fetch history stays visible. Re-classification was not cosmetic - 2026-09-03 races 2 and 4 correctly flipped CHAOS to SPLIT once only one external source remained, and source counts went 2 to 1. The recorded pick set `tests/fixtures/sources/sftb-delmar-2026-08-30.expected.json` is deliberately KEPT (user decision): it is frozen JSON that six suites use as a generic second source, never a fetch, and replacing it would force golden regeneration across engine/grading/P&L/sim/export for no behavioural gain. `docs/findings/lean-1.1-program-only.md` is also left alone - a findings file records what was true for its engine version and is not rewritten. Verification: `check-consensus`, `check-replay`, `check-classification`, `check-engine`, `check-grading`, `check-pl`, `check-sim`, `check-export`, `check-schema`, `check-equibase-otr`, `check-llm-cards`, `npm run build` all pass |
| Card sheet: per-race results panel (D81) | merged | PR [#113](https://github.com/KevinRaffay/betsheet/pull/113), branch `card-race-results` - user request 2026-09-04. `GET /api/cards/:id` gains a nested `results` object (finishers / exotics / scratches for the card's race day); `CardView.jsx` groups it by race number and renders a collapsible Results panel directly below each race's Entries panel, only for races that actually have a finisher. Nested rather than flattened because the card document already has a top-level `scratches` - the footer's PROGRAM-time scratches - and the chart's scratches are a different set. Collapsed by default like its neighbours, with a summary that reads closed ("Results (8 finishers) · won by #6 Be a Goldfish"); shows the full finish order rather than only the WPS placings, since a graded sheet is read to find out why a ticket missed. Prices render only where a pool paid. `check-grading` +5 (payload shape, row-for-row agreement with the day results endpoint, chart vs program scratch sets kept distinct, renderable finish order, and the empty-results case before any chart is saved); `build`, `check-engine`, `check-routing`, `check-pl`, `check-export` pass; browser-verified on two real cards. Also splits the duplicated D77 ledger ID into D77-A (PR #107) and D77-B (PR #108) everywhere it was cited |
| Dev harness: stale dev stacks silently steal the ports (D80) | merged | PR [#112](https://github.com/KevinRaffay/betsheet/pull/112), branch `dev-port-preflight` - reported 2026-09-04 as "why does this keep happening?" after a fresh `npm run dev` printed `Port 8788 is already in use` / `server exited (code 1)` while vite reported itself happily running on 5176. Diagnosed from the live process tree, not guessed: TWO `npm run dev` stacks were up, an hour apart. The older one still owned 8788 and 5175, and it was running the PRE-D77-A `node --watch server/index.js` command whose API child had been respawned an hour after its parent started - D77-A's phantom-restart bug, still live in an already-running process, so the port was being continuously re-taken. `concurrently -k` only kills siblings when concurrently itself exits, so the stack had orphaned when its shell went away. The damaging part was silent: vite's default port fallback moved the NEW front end to 5176 while the API hard-failed, leaving a new client proxying `/api` to the OLD stack's server - no error anywhere, and server edits appearing not to take effect. Three fixes. (1) `vite.config.js` gains `strictPort: true` so vite refuses a taken port instead of walking to the next one. (2) `scripts/dev-preflight.js` runs BEFORE `concurrently` in the `dev` script and refuses to start when either port is held, naming the holding process, its TREE ROOT and the exact command to free it; placement is load-bearing, not stylistic - a check inside the api half was written first and lost the race, since vite binds in ~50ms and `concurrently -k` SIGTERMed the diagnosing half mid-message, leaving the least useful line on screen. (3) `npm run dev:clean` (`scripts/dev-clean.js`) finds stale stacks by port, groups them by tree root and stops each tree with `taskkill /T`; dry run by default, `--yes` kills, and it re-probes afterwards rather than claiming success. `scripts/dev-ports.js` holds the shared logic; `scripts/dev-watch.js` keeps its own preflight as a backstop for being run directly. The root-not-leaf distinction is the point: the process on the port is a supervised child, so killing it just makes its parent respawn it - which is exactly why the original incident could not be waited out. Two real bugs were found while testing the tooling against live orphans and are fixed: `probePort` bound only 127.0.0.1 and therefore called a vite-held port free (vite binds ::1 on Windows; both families are probed now), and the PowerShell lookup returned `{"value":[...],"Count":n}` rather than an array because Windows PowerShell 5.1 has no `ConvertTo-Json -AsArray` (both shapes normalized). Verified end to end against real stacks: the preflight message on a live stack names leaf PID, root PID and both ports; the printed escape hatch `BETSHEET_PORT=8790 BETSHEET_VITE_PORT=5177 npm run dev` was run and does start a clean second stack beside the first; `dev:clean --yes` stopped a genuinely supervised stack and re-probed all seven ports free; a normal `npm run dev` on free ports is unchanged (api 8788, vite 5175, no fallback). `npm run build`, `check-routing`, `check-schema`, `check-ingest` all pass. No server, shared, parser, schema or API-contract change - dev tooling plus one vite dev-server option |
| Dev harness: betsheet-alt's Equibase OTR archive isolated from main's (D78) | merged | PR [#109](https://github.com/KevinRaffay/betsheet/pull/109), branch `gitignore-alt-otr-archive` - follow-up to D77-A's dev-harness work. The betsheet-alt instance isolated its port, DB and log dir but NOT the OTR archive: `BETSHEET_OTR_ARCHIVE_DIR` already existed (`server/equibase-otr.js`) and was simply unset in `C:/repos/.claude/launch.json`, so both instances shared `data/archive/equibase-otr/`. That directory is not just storage - a confirm writes `confirmed_sha256` into the per-track `manifest.json`, which is exactly the key `batchIngestEquibaseOtr` reads to decide a file is already ingested, so a day confirmed on alt would be SKIPPED by a later `npm run ingest-otr` on main whose DB never received the cards (same shape as the shared-log-dir bug already in Gotchas). Latent, never triggered: main's manifest held zero `confirmed_sha256` entries when found. The launch config (outside this repo, so uncommittable) now points alt at `data/archive-alt/equibase-otr`; the PR is the one-line `.gitignore` entry keeping alt's uploaded PDFs out of commits, mirroring `server/logs-alt/`. Verified with `git check-ignore -v` against a real file at the exact path the server writes to. No code, schema or API contract touched |
| Bottom Line: strip the handicapper byline block (D77-B) | merged | PR [#108](https://github.com/KevinRaffay/betsheet/pull/108), branch `strip-bottom-line-byline` — user request 2026-09-04. `server/program-parser.js`'s `parseAnalysis` handed back the column's byline block ("Del Mar Bottom Line By Brad Free" + the author bio + the page numbers + the repeated masthead, 416 chars, byte-identical on all 72 affected rows) glued onto whichever race paragraph ran last on the section's final page - it prints once at the page foot, so it reads as if the handicapper had written it about that race, and it reached the card view, the Replay blind view and every LLM prompt that way. New pure `stripBylineBlock` drops from the byline to the end of the chunk; the masthead grammar requires Title-case track words (the same rule `trackFromBottomLine` uses) so the ALL-CAPS best-bet line right before it survives intact, and a masthead with no byline (the sponsor-ad blocks that trail some paragraphs) is deliberately NOT touched - a separate class of furniture, out of the reported scope. Four goldens regenerated (both program fixtures, both affected backfill meet goldens), one line changed in each - `check-backfill` re-verifies each meet golden against a fresh parse of the artifacts committed beside it. `scripts/strip-bottom-line-byline.js` (new, one-off, idempotent, dry-run by default) applies the SAME exported function to already-stored rows so DB and parser cannot diverge; run against the real corpus 2026-09-04 (72 races, DB backed up first as `data/betsheet.sqlite.pre-d77-backup` via `VACUUM INTO`, so the 4MB WAL was included) and against the betsheet-alt instance (2 races). `npm run check-program` grew 6 checks (no byline survives on either fixture, every race keeps its paragraph, the bio/page-number/masthead tail goes, the best-bet line stays, a clean paragraph is unchanged, an ad-block masthead is not stripped); `check-backfill`, `check-ingest`, `check-engine`, `check-llm-cards`, `check-schema`, `check-pl`, `npm run build` all pass. No schema change, no `ENGINE_VERSION` bump (parsed input text, not generation behavior) |
| Dev harness: phantom API restarts kill in-flight requests (D77-A) | merged | PR [#107](https://github.com/KevinRaffay/betsheet/pull/107), branch `fix-dev-watch-phantom-restarts` - bug report 2026-09-04: `[vite] http proxy error: /api/fetch/ml-sheet` / `Error: read ECONNRESET` followed by the API's listening banner, mid-ingest. Not a crash: no `uncaught_exception` in the app log, and `node --watch` was verified NOT to respawn after a thrown error or a V8 OOM abort (both print "Failed running ... Waiting for file changes") - it only respawns on a file change, and the live process tree confirmed the watch parent had spawned a fresh child at exactly the ECONNRESET timestamp. Root cause: Node's watch mode restarts on any filesystem notification for a loaded module file, and Windows fires those for metadata-only touches - reproduced with `fs.utimesSync(f, atime, st.mtime)` on `server/pl.js` (bytes and mtime unchanged), which makes `node --watch` print "Restarting"; a repo-wide `find`/AV scan/indexer/`git fetch` does the same, which is why nothing in the working tree had actually changed. Unfixable inside the server: on Windows the watcher force-kills the child (TerminateProcess; a SIGTERM handler never runs - verified), so in-flight sockets always reset. Fix in two layers: (1) `scripts/dev-watch.js` replaces `node --watch` in the `dev` script - same spawn/restart behavior, but it hashes every watched file and restarts only when a hash actually moves (`--watch-path` was rejected: it restarts on ANY change under the path and the D02 log streams live in `server/logs/`); (2) `vite.config.js`'s `/api` proxy gains an `error` handler answering 503 with the `{ error }` shape `client/src/api.js`'s `asJson` already reads, so a genuine restart shows a sentence in the UI instead of an opaque network failure, plus a one-line `[api proxy]` log; its target moves from `localhost` to `127.0.0.1` to match the server's IPv4-only bind (invariant 10). Verified: metadata-only touches of 4 loaded modules plus heavy `server/logs/` writes produce zero restarts, while a real content edit restarts and names the file; killing the API mid-request through the proxy returns the 503 JSON to the client (was a bare reset). No server, shared, schema or API-contract change - `npm run build`, `check-routing`, `check-schema`, `check-ingest` all pass |
| LLM cards: identify + display the generating model everywhere, compare by model in P/L (D76) | merged | PR [#106](https://github.com/KevinRaffay/betsheet/pull/106), branch `llm-model-identity` — user request following D75's model picker: identify which model generated a card and show it anywhere the card is shown, including P/L, with the goal of comparing performance across models. Considered the user's own suggested shortcut (fold the model into `engine_version`'s name, e.g. `llm-claude-sonnet-5`) and substituted a separate `cards.llm_model` column instead (migration 022, ordinary ALTER) after tracing `server/pl.js`'s existing engine-version selection: `?engineVersion=` is a SINGLE GLOBAL filter across every completeness bucket, defaulting to whichever engine_version the most-recently-graded card anywhere in the DB happens to carry - multiplying `engine_version`'s value space for LLM cards would have made the DEFAULT P/L view silently hide entire buckets (any bucket whose fixed engine_version string didn't match that one pick) until the user manually selected "all versions", which then POOLS every model into one blended number - the opposite of "compare performance," and a real footgun invariant 14's version-pooling language was never written to guard against. A separate column sidesteps this entirely: `engine_version` stays the constant `'llm'` for every LLM card (unaffected default-selection behavior), while `llm_model` carries the actual model, set once by `persistLlmRace` at card CREATION from `llm_card_requests.model` (already recorded per-request since D63) and never overwritten - mirrors `engine_version`'s own frozen-at-creation convention. A card's model is enforced immutable, not just displayed: saving a race whose request used a DIFFERENT model than the card's already-locked one is refused (409, "Start a new card to compare a different model") - a mixed-model card's P/L couldn't be attributed to either model cleanly, defeating the entire point. `LlmCardModal.jsx` is the primary guard (the Model picker locks to the resumed card's `llm_model` and disables, with a note, the moment a card exists), the server 409 is defense in depth. Displayed everywhere a card already showed its engine version: `CardsPanel.jsx`'s Engine column, `CardView.jsx`'s header line, and `PLView.jsx`'s per-card Engine column - all via a small shared `modelLabel()`/`MODEL_LABEL` lookup in `client/src/api.js` (no network round trip per row). The actual comparison feature: `server/pl.js`'s `GET /api/pl` response gains `byModel` on the LLM_GENERATED bucket entry - a breakdown (never a second pool - the same selected rows, grouped by `llm_model` instead of summed once) of cards/tickets/wagered/returned/P&L/ROI per model, labeled `'unknown'` for a legacy card predating the column; `PLView.jsx` renders it as an always-open sub-table right under the bucket panels. `npm run check-llm-cards` grew 9 checks: `llm_model` recorded on the card at creation and echoed on `GET /race-days/:id/cards`; the 409 model-mismatch refusal (a genuine mismatch had to be manufactured by writing directly to `llm_card_requests.model`, since the stub test path always records `'stub'` regardless of what model was requested and a real off-stub call needs a key this environment deliberately doesn't have) and its "nothing changed" follow-up; the SAME request corrected back to the locked model saving normally; and a two-card, two-model P/L scenario proving `byModel` splits correctly, each row carrying only its own card, and the rows summing exactly to the bucket total. `check-schema`, `check-pl`, `check-routing`, `check-consensus`, `check-human-picks`, `check-equibase-otr`, `check-templates`, `check-export`, `check-replay`, `check-sim`, `npm run build` all pass (regression, unaffected). Browser-verified live against the real running dev instance: opened a stored day's LLM card modal, confirmed the picker was unlocked (`llm_model` null) on two PRE-D76 cards, clicked "Start a New Card", picked Haiku 4.5, generated and saved a real race - the picker immediately locked with the "generated with Haiku 4.5" note; the Betting cards table showed "llm (Haiku 4.5)" for the new card and plain "llm" for the two legacy ones; the card sheet's header read "engine `llm` · model `Haiku 4.5`"; the P/L page's new "LLM_GENERATED by model" table correctly split the bucket into a real "Haiku 4.5" row (1 card, 3 tickets, -$50) and an "unknown" row (2 legacy cards, 47 tickets, -$78) - no console errors throughout |
| LLM cards: "Start a New Card" + model picker (D75) | merged | PR [#104](https://github.com/KevinRaffay/betsheet/pull/104), branch `llm-model-picker` — user request. `LlmCardModal.jsx` always resumed the day's LATEST `template: 'llm'` card and every Generate/Regenerate/Regenerate-All call overwrote race tickets on that same card in place - the API already supported starting a fresh card (omit `cardId`, D28 append-only), the modal just never exposed the path. New "Start a New Card" button (disabled until at least one race is generated) clears `cardId` and every per-race state locally so the next Generate mints a brand-new card; the previously-resumed card is untouched, still visible in the Betting cards table. Alongside it, a Model `<select>` (Opus 5 / Sonnet 5 / Haiku 4.5 / Fable 5.1 - `server/anthropic-client.js`'s new `SELECTABLE_MODELS`, the one place the list is defined) lets the user pick which Claude model generates the NEXT call; `server/anthropic-client.js`'s `complete()` gained a `model` param (was a hardcoded module constant `MODEL`, now the server-configured default only), threaded through `previewLlmRace`'s new `model` option, the `POST .../llm-cards/preview` route (`{race, cardId?, model?}` - an unknown id is refused 400 against `SELECTABLE_MODELS` before ever reaching the API), and a new `GET /api/llm-models` endpoint (models + the server default) the modal's picker reads instead of a second copy of the list. `npm run check-llm-cards` grew 4 checks: the endpoint's shape, an unknown model refused 400, and - since the check server deliberately carries no `ANTHROPIC_API_KEY` and the stub escape hatch never touches the `model` parameter at all - a real (non-stub, non-key) preview call proving the REQUESTED model is recorded on the `llm_card_requests` row before the "ANTHROPIC_API_KEY is not set" failure, never silently swapped for the default. `check-schema`, `check-routing`, `check-consensus`, `check-human-picks`, `check-equibase-otr`, `check-pl`, `check-templates`, `npm run build` all pass (regression, unaffected). Browser-verified live with a real `ANTHROPIC_API_KEY`: opened the modal on a stored day with an existing 1-ticket LLM card, picked Haiku 4.5 from the dropdown, generated and saved a real race (4 tickets, "Race bankroll $20 · card total $20 of $200"), confirmed the "Race 1 · Not generated" reset and the new card started empty after "Start a New Card", then confirmed the ORIGINAL card (4 tickets) was still intact in the Betting cards table, unaffected |
| Equibase OTR as a consensus source: D09 three-source rule (D74) | merged | PR [#103](https://github.com/KevinRaffay/betsheet/pull/103), branch `otr-consensus` — makes the sheet (D71/D72) a third, independent consensus source feeding D09 classification, contrarian flags and the LLM generator, reusing the archive D71/D72 already parse (D07 stands, no fetcher). Mapping: `top`=showPick, `second`=winPick, the other two box4 members = new pick_type `also` (migration 021 rebuild, never `third`), written through `server/consensus.js`'s ordinary `resolvePicks`/`storePicks`/`classifyAndPersist` in the SAME transaction as the three EQB_OTR cards. `shared/classification.js`'s CHAOS rule is redefined from "3+ distinct top picks" to "no two sources share a top pick" - identical for every pre-D74 input (nothing reclassifies), diverging only once a 3rd external source can produce 3 distinct picks where a pair still agrees (the old rule mislabeled that CHAOS). Every classification now stores `agreement` (the plurality group's size); a `CLASSIFY_UNANIMOUS='majority'` config exists for comparison but stays off by default (flipping it bumps `ENGINE_VERSION`, invariant 14). `algo_fades_favorite` gains a box-only partial-order variant (fires when the favorite is absent from a box-only source's box entirely), generic by design, not keyed to OTR's name. The LLM prompt gives OTR its own sentence (show/win/box), never folded into the generic top/2nd/3rd line. `npm run ingest-otr -- --consensus-only` backfills the third source onto the 16 already-archived days without new picker cards. Two bugs found and fixed before merge (both documented in the D74 ledger row): the box-fade rule initially leaked a ranked source's non-top rows into a phantom box (`check-sources`' SFTB fixture caught it); `check-engine.js`'s synthetic CHAOS fixture was accidentally exploiting the very classification bug this PR fixes (fixed the fixture, not the new correct behavior). `docs/findings/lean-1.1-program-only.md` gains a pre-registered addendum: the 70-day corpus has zero consensus picks so nothing there changes; the one live day (2026-09-03, card #4) classifies under the fixed rule going forward, no prior day reclassified. `npm run check-equibase-otr` grew from 79 to 94 checks, `check-classification` grew 11; full 26-script regression sweep + `npm run build` all pass. No `ENGINE_VERSION` bump |
| Equibase OTR cards: fix "If it hits" showing "—" for every ticket (D73) | merged | PR [#98](https://github.com/KevinRaffay/betsheet/pull/98), branch `fix-otr-card-est-payout` — bug report. `server/equibase-otr.js`'s `buildRaceTickets` always set `estMinCents`/`estMaxCents`/`estIsRange` null (copied from `shared/parsers/human-picks.js`'s convention - correct for a human's own pasted ticket, wrong here, an equibase-otr ticket is Equibase's suggestion, not the user's own pick - same distinction D67 drew for LLM cards). New `estimateOtrTicketPayouts(tickets, entries)`, reusing `shared/betmath.js`'s SAME validated formulas D67 already fixed the identical gap with: `winPayout` exact for win, `exactaEstimate` banded off the two shortest-priced horses in the box for exacta box. Show stays without an estimate - no validated formula exists anywhere in this codebase for it (the engine itself never produces one). `npm run check-equibase-otr` grew from 74 to 79 checks (win/exacta-box estimates populated, show stays null, one hand-computed exact figure: race 1 win #5 at 3/1 → $8.00 exact); `check-schema`, `check-pl`, `check-llm-cards`, `npm run build` all pass. Browser-verified live against an isolated server instance (this time with `BETSHEET_OTR_ARCHIVE_DIR` correctly isolated from the start): uploaded the real fixture, opened the "both" card, confirmed Win tickets show exact figures ($8/$12/$26 for races 1-3) and Exacta Box tickets show ranges (e.g. $3.25–$6.76 (est.)), Show tickets still correctly show "—" |
| Equibase OTR follow-up: upload panel, batch CLI, extraction fix (D72) | merged | PR [#95](https://github.com/KevinRaffay/betsheet/pull/95), branch `equibase-otr-ui` — three parts, one PR, all following up on D71: (1) an extraction bug found by running the D71 method over all 16 archived files instead of the one file D71 was verified against - race 9 corrupted on every 10-race day because race 10's trivia sentence sometimes prints on its own y-line above the header rather than sharing it; fixed with a trivia-line guard (broadened past the request's literal "starts with #N" wording once a second trivia template surfaced in the real corpus) plus grammar-anchored box parsing (`otr_trailing_tokens` instead of silent absorption) - see the full writeup in the D71/D72 architecture rows and the D72 DELIVERABLES.md ledger row (also caught a `String.match()` global-regex `.index` bug in the grammar helper itself while verifying). All 16 files (143 races) now parse clean; all 16 committed as fixtures. (2) `npm run ingest-otr -- [dir]` batch CLI, policy A (D43), idempotent by sha256. (3) `EquibaseOtrPanel.jsx`, the upload panel D71 deferred, styled to match ConsensusPanel's At The Races PDF upload (D69) - the closest real precedent, substituted for the request's file references which don't describe an existing pattern. `npm run check-equibase-otr` grew from 37 to 74 checks; `check-schema`, `check-pl`, `check-distribution`, `check-human-picks`, `check-templates`, `check-llm-cards`, `check-replay`, `check-routing`, `check-ingest`, `check-consensus`, `npm run build` all pass. Browser-verified the panel end to end against an isolated server instance (file input driven via a `DataTransfer`-constructed `File` since the in-app Browser pane can't drive a native OS file chooser): preview, confirm (3 cards), re-upload (append-only notice, 6 cards total), and the D07 mismatch error all rendered correctly |
| Equibase "Off to the Races" PDF as a picker (D71) | merged | PR [#92](https://github.com/KevinRaffay/betsheet/pull/92), branch `equibase-otr` — new phase-3 picker source: the free at-track sheet's printed tickets, taken verbatim, become a card in the new `EQB_OTR` bucket, graded and compared like HUMAN/LLM_GENERATED. D07 stands: no fetcher, manual PDF upload only. Extraction (`shared/parsers/equibase-otr.js`) verified byte-for-byte against the real 2026-09-03 Del Mar file before any code was written - `pdftotext -tsv` word coordinates split into the two printed tiers at x=360 (`OTR_COLUMN_BOUNDARY`); `-layout` interleaves the tiers and plain `pdftotext` scrambles race order, neither usable. `server/equibase-otr.js`: archives every upload under `data/archive/equibase-otr/<TRK>/<date>.pdf` + a manifest (D41 pattern, overridable via `BETSHEET_OTR_ARCHIVE_DIR` so checks never touch the real archive), preview-then-confirm (invariant 9) via a `parseToken` = the archived file's own sha256 so confirm always re-parses the ARCHIVED bytes rather than trusting the client, three cards per confirm (`some-reward` $14/race, `higher-reward` $14/race, `both` $28/race - append-only, consecutive card_numbers), new `EQB_OTR` completeness value (migration 020, CHECK-rebuild, same pattern as 016/018) and `template`/`engine_version` `'equibase-otr'`. Per-ticket blocking on an unknown program number (never the whole race) mirrors `shared/parsers/human-picks.js`'s severity contract. Two verified deviations from the user's spec, both matching the D69 precedent of substituting the codebase's real established convention over a described one that doesn't exist here: (1) upload is a raw `application/pdf` body, not multipart (no multipart dependency exists anywhere in this codebase); (2) the sheet's track and date print on two SEPARATE lines in the real file, not one combined header line. `PDF_TSV` extraction requires Poppler's `pdftotext`, not xpdf (this dev machine's PATH `pdftotext` is xpdf 4.06 and silently lacks `-tsv`) - `resolvePdftotextCommand()` locates the winget Poppler install automatically and sanity-checks the TSV header so a wrong binary throws instead of silently mis-parsing. `npm run check-equibase-otr` (new, 37 checks): golden parse + structural invariants on all 8 real races, header trivia contributing no picks, synthetic wrapped-box-horse and structure-mismatch edge cases, full upload→preview→confirm round trip with exact ticket counts/costs ($112/$112/$224), append-only re-upload, per-ticket blocking, the D07 track/date mismatch refusal, P/L bucket isolation against the day's own lean/HUMAN/LLM_GENERATED cards, archive+manifest persistence with an identical re-parse from the archived file, and the no-engine-version-bump identity. `check-schema`, `check-pl`, `check-distribution`, `check-human-picks`, `check-templates`, `check-llm-cards`, `check-replay`, `check-routing`, `npm run build` all pass. No client UI component in this PR (the user's spec described only the server/parser/reporting surface, unlike D54/D69 which named UI files explicitly) and no `consensus_picks` writes - both explicitly out of scope per the spec, the latter gated on a future D09 three-source classification decision (intended mapping: top=showPick, second=winPick, box members unranked - recorded here so the follow-up doesn't re-derive it) |
| LLM cards: "Regenerate All Races" button in the modal (D70) | merged | PR [#90](https://github.com/KevinRaffay/betsheet/pull/90), branch `llm-regenerate-all` — user request. Runs the existing per-race generate-then-save operation (`previewLlmCard` -> `lockLlmCard`, the same call `handleGenerate`/`handleSave` already make for one race) across every race on the day in race-number order, auto-saving each without the manual "Save this race" click, and refreshing `ticketsByRace` live after each race so the panel updates race by race rather than waiting for all 8 to finish. A race whose preview comes back with a BLOCKING warning is left untouched (whatever ticket it had, if any) and the run continues to the next race - one bad race never stops the rest, matching this codebase's existing "a missing/bad source never blocks the whole day" pattern (D07, D69). A network/server error on one race is caught the same way. A results summary ("Regenerate all: N of M races saved", with a warning list of any blocked/failed races and why) renders after the run completes. `cardId`/`correlationId` are tracked in local variables during the loop (not the React state setters, which don't apply synchronously mid-loop) so the very first race's save can create the card and every subsequent race in the same run reuses it correctly. Presentation/orchestration-only - no server or schema change, reuses the exact API calls the single-race flow already makes. `npm run build` passes; browser-verified end to end with a real `ANTHROPIC_API_KEY` on the real 8-race Del Mar 2026-09-03 day (no prior card): clicked "Regenerate All Races", watched the progress line advance "Regenerating race N (N of 8)…" and each race's panel populate with real reasoning/tickets live as it completed, final summary read "Regenerate all: 8 of 8 races saved", Betting cards table showed the new card (31 tickets, $198 of $200 bankroll), footer correctly flipped to "All races generated - Close" |
| At The Races racecard PDF upload as a consensus source (D69) | merged | PR [#89](https://github.com/KevinRaffay/betsheet/pull/89), branch `atr-pdf` — replaces the per-race manual paste for ATR with one same-day "print to PDF" of `attheraces.com/racecards/<track>/<date>` covering every race. New pure parser `shared/parsers/atr-pdf.js` (`parseAtrPdfText`): splits on the authoritative `Race N - ` header, extracts `Top Tip: NAME (pgm)` / `Watch out for: NAME (pgm)` per block, warns `missing_verdict` (non-blocking) on a block missing the pair - mirrors invariant 3's "missing consensus never blocks generation". v1 does not extract the Verdict paragraph's optional third horse, same limitation already accepted for the pasted Verdict format. The user's spec named `shared/parsers/program-pdf.js`/`chart-pdf.js` and a `pdftotext -layout` convention as precedent to reuse; neither exists in this codebase (confirmed: zero `pdftotext` matches repo-wide, poppler-utils not installed in this environment) - substituted the real equivalent, `server/pdf-text.js`'s `extractPdfLines` (pdfjs-dist line-reconstruction), which the chart-PDF upload path already feeds into its parser the identical way; verified byte-for-byte against a real Sep 3 2026 Del Mar PDF before writing any code, all 8 races matching the spec's worked examples exactly. Server wiring in `server/consensus.js` (not a new file - lives beside the manual-paste routes it mirrors): `POST /race-days/:id/consensus/atr-pdf-preview` (raw `application/pdf` body, same `express.raw` pattern as `server/ingest.js`'s `/parse/results-pdf`) and `POST /race-days/:id/consensus/atr-pdf` (confirm), same preview-then-write-nothing/confirm contract as manual paste (invariant 9), feeding the SAME `consensus_picks` table via a small adapter (`atrPicksToRaces`) into the existing `resolvePicks`/`storePicks` - no new schema. Fetch-audit row uses `outcome: 'manual_upload'`, which already existed in the `fetch_attempts` CHECK constraint and in read-side "sources used" filtering (`server/cards.js`, `ConsensusPanel.jsx`, `server/trace-export.js`) since migration 001 but had never actually been written anywhere until this PR. `ConsensusPanel.jsx` gets a sibling upload panel next to manual paste, same preview table/warnings/confirm UI shape. Fixture: the real PDF committed at `tests/fixtures/atr-pdf/delmar-2026-09-03.pdf` (1MB) - same real Del Mar 2026-09-03 card as `tests/fixtures/entries/dmtc-2026-09-03.txt`, so program numbers resolve to real entries in checks. `npm run check-atr-pdf` (new, 14 checks): all 8 real races extract exactly; a synthetic missing-Top-Tip block warns `missing_verdict` for that race only, others unaffected; header/footer noise between blocks never bleeds into the wrong race; full upload -> preview -> confirm -> `consensus_picks` rows -> `fetch_attempts` audit row round trip against the real server and real entries. `check-schema`, `check-consensus`, `npm run build` all pass. Browser-verified live: uploaded the real PDF through the actual file input, previewed (all 8 races, zero warnings, no unmatched picks), confirmed, watched the Consensus table populate with all 8 races' top/watch picks and every race's classification flip to SPLIT with 1 external source |
| LLM cards: regression test proving consensus picks reach the model prompt (D68) | merged | PR [#88](https://github.com/KevinRaffay/betsheet/pull/88), branch `test-llm-consensus-in-prompt` — user follow-up 2026-09-03, after a real bug surfaced in review: `shared/classification.js`'s `buildConsensusTable` returns the table array directly, not `{table: [...]}`; `server/llm-cards.js` (D63) called it as `.table`, so `consensusTable` was silently `undefined` on every call and every LLM prompt read "No external consensus on file" even with real consensus on the day - fixed independently via a `qa`-branch commit that dropped the stray `.table`. This is the missing regression coverage, test-only: `npm run check-llm-cards` grew a block that seeds a real consensus pick through the actual manual-paste API (not a hand-built table object) and inspects the ACTUAL `prompt_text` logged to `llm_card_requests`. Verified the test catches the bug by temporarily reintroducing `.table`, confirming the suite fails and shows the broken prompt, then reverting. `npm run check-llm-cards` passes |
| LLM cards: fix "If it hits" column empty on the card sheet (D67) | merged | PR [#84](https://github.com/KevinRaffay/betsheet/pull/84), branch `fix-llm-card-est-payout` — live bug report 2026-09-03. Root cause: `shared/parsers/human-picks.js` (reused for LLM tickets since D63) always sets `estMinCents`/`estMaxCents` null - correct for a HUMAN's own pasted picks (D54, no estimate needed), wrong for an LLM's, which should be reviewable like the engine's own cards. Fix scoped to LLM cards only: new `estimatePayouts` in `server/llm-cards.js`, called at save time, reuses `shared/betmath.js`'s SAME validated formulas the engine uses (`winPayout` exact for win, `placeEstimate`/`exactaEstimate`/`trifectaBoxEstimate` banded for place/exacta/exacta box/trifecta box - exacta box using the two shortest-priced horses in the box). Show/straight trifecta/superfecta/superfecta box deliberately left without an estimate - no validated formula exists anywhere in this codebase for them (the engine never produces those types), and inventing new unvalidated multipliers alongside `betmath.js`'s carefully-tuned constants would be guessing with money math. `npm run check-llm-cards` grew two regressions (a win ticket's exact estimate; an exacta box estimate using the two shortest-priced horses, independently computed and compared); `check-human-picks` (confirms human cards unaffected), `check-schema`, `npm run build` all pass. Browser-verified end to end with a real `ANTHROPIC_API_KEY`: generated/saved a 3-ticket race, opened the real card view, confirmed "If it hits" reads $280 exact (win), $60.00–$90.00 est. (place), $62.56–$129.94 est. (exacta box) - all matching hand-computed expected values |
| LLM cards: modal's saved races show a collapsible card panel per row; "Open card #N" removed (D66) | merged | PR [#83](https://github.com/KevinRaffay/betsheet/pull/83), branch `llm-card-collapsible-panels` — user request 2026-09-03: once a race is generated, its card should be visible in a collapsible panel in that race's row, and the "Open Card #" button is no longer needed. `LlmCardModal.jsx`: every already-generated race carries its own `<details>` panel (independently open/closed via an `expandedRaces` Set), sourced from the card's actual saved tickets (`GET /api/cards/:id`), not the ephemeral live-preview state - so a race's card stays viewable/collapsible after moving on to the next one, not just briefly after saving. The just-saved race's panel auto-opens. A normalized `TicketsTable` helper renders a ticket identically whether it came from a live preview (camelCase) or a saved DB row (snake_case), removing the duplicated table markup the D65 version had. "Generate next race" / "All races generated - Close" moved to the modal's persistent bottom action bar (previously lived inside the just-saved row, which no longer exists as a distinct state). `onOpenCard` dropped from both the modal and `CardsPanel.jsx`'s usage of it. Presentation-only - no server/schema change. `npm run build` passes; `check-llm-cards`/`check-routing` (untouched) still pass as a sanity check; browser-verified end to end with a real `ANTHROPIC_API_KEY` - generated and saved both races of a seeded day, confirmed each race's panel opens/collapses independently (collapsed race 1's, generated race 2, both panels stayed correctly independent), table updated live each time, zero console errors |
| LLM cards: "Generate Card from LLM" as a modal; fixes the Betting cards row going stale (D65) | merged | PR [#82](https://github.com/KevinRaffay/betsheet/pull/82), branch `llm-card-modal` — user request 2026-09-03. The inline `LlmCardPanel.jsx` toggle is replaced by `LlmCardModal.jsx`: Generate a race -> preview displays -> Save -> the SAVED ticket stays shown read-only ("Saved ✓") with a "Generate next race" button, or "All races generated - Close" once every race has a ticket. Root cause of the stale-row bug: the old panel's `handleSave` only refreshed its own local state, never told `CardsPanel.jsx` its Betting cards table (ticket count, day total) was stale. Fix: an `onCardChanged` prop wired straight to `CardsPanel.jsx`'s own `reload`, called after every save so the table updates live while the modal is still open. Presentation-only - no server/schema change. `npm run build` passes; browser-verified end to end with a real `ANTHROPIC_API_KEY` (generated + saved both races of a seeded day, watched the table update live mid-dialog each time, confirmed resume-on-reload, zero console errors) |
| LLM cards: box-bet prompt fix - total didn't divide evenly per combo (D64) | merged | PR [#81](https://github.com/KevinRaffay/betsheet/pull/81), branch `fix-llm-box-stake-prompt` — live bug report: the real 2026-09-03 Del Mar day's race 1 produced `exacta box #2,#4,#3 $16.50` on a `$1 Exacta` race (6 combinations, $2.75/combo, not a whole dollar), correctly blocked by `shared/parsers/human-picks.js` but with no way for the model to have gotten it right - the prompt never explained box combination counts or the divisibility rule. Prompt-only fix (`server/llm-prompt.js`'s `SYSTEM_PROMPT`, `docs/prompts/llm-card-v1.md` kept in sync with a dated Fixes entry): gives the exact combination-count formula per box type plus a worked example using this exact failure. No parser/server/schema change - the blocking behavior is unchanged and remains the real backstop. `npm run check-llm-cards` grew a prompt-content assertion plus a regression reproducing the live report verbatim through the stubbed pipeline, confirming the identical blocking message and refused save; `npm run build` unaffected (no client change) |
| LLM cards: manual per-race LLM picks as a third comparison point (D63) | merged | PR [#79](https://github.com/KevinRaffay/betsheet/pull/79), branch `llm-cards` — "Generate Card from LLM" alongside the existing "Generate card" button, per race: preview (the actual model call, logged verbatim to the new `llm_card_requests` table regardless of outcome) then an explicit Save, same D54 preview-first shape. New `LLM_GENERATED` completeness bucket (migration 018 rebuild) and `engine_version: 'llm'`/`template: 'llm'` (never pools with any engine bucket or with HUMAN). The response's ticket block reuses `shared/parsers/human-picks.js` unmodified (new optional `ruleTag` param) rather than a new JSON schema/parser. Per-race bankroll defaults to `(card bankroll - spent so far) / (races remaining)`, recomputed on every call - flagged for review, two alternatives (flat split; user-editable) rejected, see docs/prompts/llm-card-v1.md. `server/anthropic-client.js` reuses life-swipe's `server/anthropic.js` client pattern exactly (no SDK, no provider-switch seam - not needed here). `npm run check-llm-cards` (new) never calls the real API - `BETSHEET_LLM_TEST_MODE=1` lets a request carry a stubbed response straight through the real preview/save/parse path; `check-schema`, `check-human-picks`, `check-pl`, `check-distribution`, `check-templates`, `check-routing`, `check-replay`, `check-sim`, `check-export`, `check-grading`, `check-ingest`, `npm run build` all pass. Browser-verified the no-key failure path live (no `ANTHROPIC_API_KEY` configured in this environment): "Generate" on a race shows a visible error, "LLM call failed: ANTHROPIC_API_KEY is not set." — no crash, no partial card; the success path is covered by the stubbed server round trip instead |
| Per-race Del Mar Bottom Line on card view (D48-QA) | merged | QA `qa` branch deliverable — migration 012 stores nullable race-level Bottom Line text; ingest preserves parser analysis, the card API returns it with races and entries, and `/card/:id` renders available text in collapsed panels; validated with `npm run check-schema`, `npm run check-ingest`, `npm run check-program`, `npm run check-engine`, and `npm run build` |
| Hard-delete individual card (D49-QA) | merged | QA `qa` branch deliverable — confirmed `/card/:id` action and `DELETE /api/cards/:id` remove card-owned records through foreign-key cascades while preserving the race day and decision logs; validated with `npm run check-engine` and `npm run build` |
| Repo scaffold (D01) | merged | PR #1 — stack, styling system, server wiring, docs ledger |
| Logging foundation (D02) | merged | PR #2 — three JSONL streams, size+day rotation, gzip/retention sweep, correlation IDs, `/api` request log |
| SQLite schema + migrations (D03) | merged | PR #3 — full schema incl. Phase 2–4 tables, append-only migrations with tamper guard, money in cents |
| Entries parser — pasted text (D04) | merged | PR #4 — validated against a real Del Mar card (8 races, 81 entries, 0 warnings) |
| Ingest UI + API (D06) | merged | PR #6 — paste/PDF → warnings-first read-only preview → transactional save; migration 002 adds `races.wager_menu` |
| Consensus-fetch framework (D07) | merged | PR #7 — fetcher registry, robots/backoff/mismatch handling, audit trail, manual paste fallback w/ read-only preview |
| Program track fallback (D31) | merged | PR #28, branch `program-track-fallback` - the 2026-08-22 Del Mar program has no panel payoff box, so the parser returned track null and "Save race day" sat disabled with no explanation. Parser now falls back to the "<Track> Bottom Line" header (else warns `no_track`); the ingest preview names the missing field. Known follow-ups on that program (not fixed here): stakes race titles without the word "Stakes" (Green Flash Handicap, Del Mar Oaks, Del Mar Mile) fall back to the conditions text; R3 distance null, R7/R11 distance mis-picked |
| Bankroll balancer remainder fix (D30) | merged | PR #27, branch `balancer-remainder` - the balancer parked the whole per-card rounding remainder on the single biggest win ticket (card 16 R1: $37 allocated, $65 spent, a quarter of the bankroll on one horse). Now round-robin $1 steps per race per pass, largest allocations first, guesswork races last; `remainder_distributed` trace event; card view shows allocated AND spent per race |
| Place-money carve-out (D36) | merged | PR #38, branch `place-money-carve-out` - engine `lean-1.1`: the pair is sized inside the allocation, the balancer tops deficits up toward each allocation before spreading, `remainder_distributed` names skipped races; check-engine allocation-integrity assertions |
| dmtc results HTML parser (D42) | merged | PR #37, branch `dmtc-results-parser` - second results source of record, cross-validated against Equibase; from-archive preview with the calendar race-count hard error; provenance vocabulary (migration 010) |
| Batch backfill runner + Backfill queue (D43) | merged | PR #41, branch `backfill-runner` — migration 011 (`race_days.meet`, `backfill_queue`), server/backfill.js (policy A in code, commitDay through the routes' own writers, golden checkpoint per meet, cross-source in memory, per-meet report), queue API + view, P/L by meet, `npm run backfill`, `npm run check-backfill`; the routes now call the shared `insertRaceDay` / `saveResults` / `persistCard` |
| Backfill run DMR-2026-summer (D44) | merged | PR #42, branch `backfill-dmr-2026-summer` — 27 days Jul 17 - Aug 30 saved under lean-1.1, zero queued, golden 2026-07-17 committed, cross-source identical on 08-28/29/30, report docs/backfill/DMR-2026-summer.md; every backfilled card is PROGRAM_ONLY (no consensus source runs on archived days) |
| Index source order + meet-dates table (D47) | merged | PR #43, branch `backfill-dmr-2025-summer` — indexRaceDays / probeMeetWindow / dmtc-probe, three-way race-count rule in the runner, dark 2025-07 calendar fixture |
| Backfill run DMR-2025-summer (D45) | merged | PR #44, branch `backfill-dmr-2025-summer-run` — meet-dates table from the probe (31 race days), parser fixes for the 2025 print run (ad slug overprint, footer-anchored panel edge, 8pt numbers on 15-horse fields), golden 2025-07-18 committed, 31 saved / 0 queued, report docs/backfill/DMR-2025-summer.md |
| Backfill run DMR-2025-fall (D46) | merged | PR #47, branch `backfill-dmr-2025-fall` — meet-dates table from the probe (14 race days), foreign_program detection (the Breeders' Cup official program on BC days; blocks -> queue -> confirmed sheet-only as ODDS_ONLY), golden 2025-10-31 committed (BC program by digest only), 12 saved + 2 resolved then dropped, report docs/backfill/DMR-2025-fall.md |
| Distribution reporting (D20) | merged | PR #51, branch `distributions` — shared/distribution.js + GET /api/distribution + Distributions view; per bucket: % losing days, max drawdown, single-ticket dependence gross and net (flag = net > 80%); one card per day per bucket; `npm run check-distribution` |
| Replay: day landing page listing every race with P/L before opening one (D62) | merged | PR [#80](https://github.com/KevinRaffay/betsheet/pull/80), branch `replay-day-landing` — clicking a day in `/replay` opens `ReplayDayLanding.jsx` (a table of every race, status, and human/lean P/L once revealed) instead of jumping straight into race 1; clicking a race row renders `ReplayRaceView` for that race in place, same `/replay/day/:id` URL, no route change. New `GET /replay/days/:id/races?cardId=` (server/replay.js). `ReplayRaceView.jsx` gained an `initialRace` prop (default 1). `npm run check-replay` grew coverage for the new endpoint; `check-routing`, `npm run build` pass; browser-verified end to end on a seeded day |
| Replay: removed the DMR-2025-only default + "widen to every meet" checkbox (D61) | merged | PR #78, branch `replay-remove-meet-pool` — user request 2026-09-02. `/replay` now always shows every stored day with results; `GET /replay/days` and `GET /replay/random` (server/replay.js) dropped the `?pool=recent\|all` parameter and its `meet?.startsWith('DMR-2025')` filter entirely (not just hidden in the UI - removed server-side too, since nothing calls it anymore); `client/src/api.js`'s `getReplayDays`/`getRandomReplayDay` take no argument. `npm run check-replay`, `check-routing`, `npm run build` all pass; browser-verified all three stored days (incl. a 2026 one) show with no checkbox |
| Replay: the actual betting card, ticket by ticket, once the day is closed (D60) | merged | PR #77, branch `replay-show-graded-card` — user request 2026-09-02: after Finish Order, show the actual card with payouts, not just a one-line race P/L. `server/replay.js`'s `revealedPayload` now carries `tellerCall`/`rationaleText`/`oddsAtBet` on each graded ticket (grading itself untouched - the display fields ride along, never read by `gradeCard`); client renders a new `GradedTicketsTable` (bet type, selections/rationale, teller call, cost, outcome + returned, P/L, subtotal) for human and lean, plus the exotic payoffs table (fetched since D55, never rendered until now) - all gated on `summary.closed`, not merely `revealed`, per the request. Fixed a real gap surfaced while building this: `summary` was previously only ever set by clicking Close IN THAT TAB, so a genuinely closed day would show as never-played after a reload or on a fresh visit; `ReplayRaceView.jsx` now fetches `summary` from the server on mount and after every reveal, and resumes the day's own latest human card via `listCards` on mount too - both necessary for "after the day is closed" to mean anything across a real browsing session, not just one uninterrupted paste-lock-reveal-close run. `npm run check-replay` grew an assertion that a graded ticket's `tellerCall` survives the round trip; `check-human-picks`, `check-schema`, `check-routing`, `npm run build` all pass; browser-verified against the real closed card built during D55/D58 testing (both cards' full tickets rendered correctly, resumed after a fresh page load with no prior session state) |
| Backfill: races.bottom_line was silently dropped, now fixed + retroactively filled (D59) | merged | PR #76, branch `fix-backfill-bottom-line-drop` — root cause of D58's "no stored day has bottom_line" observation: `server/backfill.js`'s `dayPayload()` built the save payload from `parsed.merged` but never copied `parsed.merged.analysis` onto it, so `insertRaceDay`'s `bottomLineByRace` map (keyed on `payload.analysis`) was always empty for every batch-backfilled day - the live single-day flow (NewRaceDay.jsx) was never affected, only the D43 batch runner. One-line fix (`analysis: parsed.merged.analysis ?? []`); `scripts/backfill-bottom-line.js` (new, one-off, idempotent, dry-run by default) retroactively re-parses each already-saved day's archived program and fills the column. Run against the real corpus 2026-09-02: 650 races across 70 days, zero skipped (archive fully intact); DB backed up first (`data/betsheet.sqlite.pre-d59-backup`). `npm run check-backfill` grew an assertion on the real 08-30 day proving `bottom_line` now populates; `check-schema`, `check-ingest`, `check-pl`, `npm run build` all pass; browser-verified on betsheet-alt against the real backfilled 2026-08-30 data |
| Replay: Bottom Line shown open by default (D58) | merged | PR #75, branch `replay-bottom-line-visible` — user request 2026-09-02: the blind race view already returned and rendered `bottom_line`, but inside a `<details>` with no `open` attribute (matching CardView.jsx's own convention), so it read as missing unless clicked open. Now `open` in ReplayRaceView.jsx only - a cosmetic, one-attribute fix; no server/schema change. Verified visually against injected test data on the betsheet-alt instance (no stored day in either DB currently has bottom_line populated) since real ingest hasn't hit that path in this corpus yet; `npm run check-replay`, `check-routing`, `npm run build` all pass |
| Human ticket parser: pipe-delimited rows (D57) | merged | PR #73, branch `add-pipe-delimiter`, stacked on `fix-box-selection-separator`, merged to `main` via PR #74 — a plain browser textarea's Tab key moves focus rather than inserting a tab character, so the original tab/2+-space format was only reliably enterable by pasting from a spreadsheet, not by typing a row by hand. `|` is now an accepted column delimiter, detected per line (never mixed with the tab/2+-space fallback in one row, so an incidental double space inside a piped rationale is never mistaken for a column break); ReplayRaceView.jsx's paste box now recommends it. `npm run check-human-picks` grew 3 cases (parity with the tab-delimited design-note table, a rationale with an internal double space, the plain tab fallback still works); browser-verified typing a pipe-delimited row directly into the textarea |
| Human ticket parser: box selections accept '/' as well as ',' (D56) | merged | PR #72, branch `fix-box-selection-separator` — bug fix to D54, found live in Replay: `Exacta Box #1 / #6 $25` was read as ONE selection token (box types only split on ','), producing a spurious name-mismatch and a blocking "needs more horses". Box selections have no positional meaning, so '/' and ',' are now equivalent separators there; `npm run check-human-picks` grew a regression case |
| Replay: blind race view, lock/reveal, human-vs-lean standing (D55) | merged | PR #68, branch `replay`, stacked on `human-cards`, merged to `main` via PR #70 — shared/replay.js (`computeBlindness`, `isCardClosed`, `pickerAgreement`, pure) + server/replay.js (day picker, blind view with strict key-set discipline pre-reveal, reveal, `POST .../close`, standing grouped by blindness + classification-seen, never pooled), migration 017 (`cards.saw_classification`, one-way toggle), three new components (ReplayDayPicker/ReplayRaceView/ReplayStanding). No schema/behavior change to D54's writer - the UI calls its endpoints directly. Invariant 15 added. Browser-verified end to end on a fresh fixture day: paste → preview → lock → reveal (human vs. lean side by side) → classification toggle → PASS → close (correctly computed SEQUENTIAL) → standing table. `npm run check-replay` (new), `check-human-picks`, `check-schema`, `check-routing`, `npm run build` all pass |
| Human cards: paste parser, preview, HUMAN bucket (D54) | merged | PR #67, branch `human-cards` — shared/parsers/human-picks.js (pure, per-warning `blocking`) + server/human-cards.js (previewHumanRace/persistHumanRace, the one writer), migration 015 (`tickets.rationale_text`/`odds_at_bet`, `race_days.replayed_at`, `human_race_state`) + 016 (rebuild: `consensus_completeness` admits HUMAN; verified against a copy of the real 70+-day DB, zero FK errors, every pre-existing row count unchanged), `template: 'human'` seeded outside shared/templates.js's TEMPLATES map (never engine-generated or simulated), invariant 13 extended, P/L gains ROI-on-bankroll beside ROI-on-wagered (a PASS-heavy card can't look better than full coverage by construction). No engine/grader change — `npm run check-engine` stays byte-identical. `npm run check-human-picks`, `check-schema`, `check-pl`, `check-distribution`, `check-templates`, `check-sim`, `npm run build` |
| Track name canonicalization (D35) | merged | PR #64, branch `track-canonicalization` — shared/track-codes.js (`canonicalizeTrack`, pure), migration 014 (`race_days.track_code`, backfilled), `insertRaceDay` canonicalizes at save, the one-day-per-track+date check and results-chart mismatch refusal key on `track_code`, `meetForDay` drops its ad hoc letters-only check for the shared one, parse previews warn (never block) on an unrecognized track; `npm run check-schema`, `check-ingest`, `check-consensus`, `check-sources`, `check-dmtc-fetch`, `check-backfill`, `check-ml`, `npm run build` |
| SFTB discovery fix + not_published (D53) | merged | PR #62, branch `sftb-discovery` — found live 2026-09-02: a Del Mar 2026-09-03 fetch logged http_error "no published page found" with no URL, status or discovery detail while the post existed; cause = the track spelling ("Delmar" slugs to delmar-..., the site says del-mar-...; D35 is the real fix). Fetcher normalizes the track for now, every attempt row carries url / httpStatus / sitemap / candidate slug / entries scanned / nearest slug, the miss is `not_published` (migration 013) and never counts toward the backoff; `npm run check-sources`, `check-consensus`, `check-schema` |
| Findings: lean-1.1 on the PROGRAM_ONLY corpus (D52) | merged | PR #58, branch `findings-lean-1-1` (stacked on #57) — docs/findings/lean-1.1-program-only.md from runs #27-#50 (every template, both scratch modes, all meets and per meet, paired day counts); the "Findings" section above with the per-version rule; no code |
| SimView compare follow-up (D51) | merged | PR #57, branch `sim-compare-view` (stacked on #56) — meet selector, vs lean column with paired day counts, drawdown deltas, the Simulated templates section on Distributions; read-side only, no schema change, ENGINE_VERSION lean-1.1; `npm run check-sim` +8 |
| Simulator chart-scratch mode (D50) | merged | PR #56, branch `sim-chart-scratches` (stacked on #55) — run option `applyChartScratchesBeforeGeneration`: cards built at the window, grading unchanged, mode stored on the run and grouped in the compare (never pooled), Mode column in SimView, trace flag; no engine change, ENGINE_VERSION lean-1.1; `npm run check-sim` +7 |
| Exotic-isolation templates (D49) | merged | PR #55, branch `exotic-isolation-templates` — two simulation-only templates, box-only (`midPriceCoverage: false`) and straight-only (`hedgeBoxDepth: 0`, the one engine edit: depth 0 declines the split box as `disabled_by_template`, lean's depth-2 path untouched); four distinct ticket sets with lean and no-exotics on the PROGRAM_ONLY fixture, D36 tolerance held, lean byte-identical, ENGINE_VERSION stays lean-1.1 |
| Program-rank templates + rule_suppressed (D48) | merged | PR #52, branch `program-rank-templates` — four simulation-only templates (exacta-primary, no-exotics, box-depth-3, best-bet-weighted) on new template-driven knobs; rule_suppressed with machine-readable reasons at every evaluation site; lean byte-identical, no version bump |
| dmtc.com crawler + raw archive (D41) | merged | PR #36, branch `dmtc-crawler` - calendar-indexed, polite, conditional, audited crawler; raw archive with manifests; `npm run dmtc-fetch`; the two real calendar pages are fixtures |
| ML sheet as entries source of record (D40) | merged | PR #34, branch `ml-sheet-ingest` - ML/changes PDF parser + fetcher, program becomes analysis-only via the merge, ODDS_ONLY tier, engine `lean-1.0.1` (morning-line ranking fallback on rank-less races) |
| Engine versioning (D34) | merged | PR #33, branch `engine-versioning` - `ENGINE_VERSION = lean-1.0`; migration 008; version on card rows, card header, P/L rows + version filter; trace events carry it; invariant 14 |
| Simulator (D19) | merged | PR #31, branch `simulator` - every template replayed over every stored day with a chart; per-bucket compare + bankroll over time; runs append-only; `npm run check-sim` |
| Browser routes (D44-QA) | merged | History API routes for `/`, `/new`, `/pl`, `/simulate`, `/day/:id`, and `/card/:id`; production server serves the client shell for direct navigation; `npm run check-routing` — PRs #45, #48 from `qa` |
| Fused owner/trainer split (D33) | merged | PR #30, branch `owner-trainer-split` - 08-22 R9 #9 Kensington Lane arrived with "Philip D' Amato(M. Donald)" fused into the owner item and trainer null (pinned as a known gap by D32). Card-wide dictionary pass; the 08-22 golden changes in exactly that entry's owner/trainer, 08-30 byte-identical |
| Program header fields (D32) | merged | PR #29, branch `program-header-fields` - on the 2026-08-22 program, stakes titles without the word "Stakes" (Green Flash Handicap, Del Mar Oaks, Pacific Classic, Del Mar Mile) fell back to conditions text as the race type, and three distances were wrong (R3 null on a line wrap, R7 read from conditions text, R11 from the title) plus R10 truncated to "One Mile" (Pacific Classic is a mile and a quarter). Rules rewritten as pure helpers pinned by the exact pdfjs text items; the 08-22 program is now the SECOND committed fixture with its own golden + 19 hand-checked assertions (branch carries D31 merged in, since the golden needs the track fallback) |
| Strategy templates (D18) | merged | PR #26, branch `strategy-templates` — 6 named rule bundles + signal/structure layer map; allocationCurve made real (lean/spread); invariant-1 422 guard on the live endpoint; template on card rows, trace, P/L and export |
| Decision-trace export (D17) | merged | PR #25, branch `trace-export` — Phase 3 opens; per-card JSON feed (trace ⋈ grades ⋈ results), traceStatus honesty marker, CLI twin, docs/trace-schema.md |
| P/L views (D16) | merged | PR #24, branch `pl-views` — bucket panels + running card table + per-race variant-compare matrix; invariants 12/13 enforced by check-pl |
| Ticket grading engine (D15) | merged | PR #23, branch `grading` — shared/grading.js (pure) + server/grading.js; results save auto-grades every card; Result/P&L columns on the card view; validated on the real Del Mar day's printed payoffs |
| Results ingest UI (D14) | merged | PR #21, branch `results-ingest` — paste/PDF -> read-only preview -> persist; mismatched charts refused whole |
| Chart-PDF ingestion (D13) | merged | PR #20, branch `chart-pdf` — pdf-text extraction + results parse endpoints (preview-only) |
| Results-chart parser (D12) | merged | PR #19, branch `chart-parser` — Phase 2 opens; the real Equibase chart for the fixture day; program↔chart closure proven |
| Card recipes + append-only generation (D28) | merged | PR #17, branch `card-recipes` — migration 007 (cards rebuild: AUTOINCREMENT id, per-day card_number, per_race_min_cents, UNIQUE(day,variant) dropped); list shows the full recipe |
| Factory reset (D27) | merged | PR #16, branch `app-reset` — wipe all records + all logs behind explicit confirms (API token, CLI --yes, UI Danger zone); id sequence restarts; `app_reset` opens the new era's log |
| Race-day soft delete (D26) | merged | PR #15, branch `race-day-delete` — deleted_at + filter cascade (invariant 12), confirmation dialog with counts, deleted list + restore, 410 guards on mutations, deletion/restore logged to decision-trace, logs never touched |
| Desktop card view (D11) | merged | PR #14, branch `card-view` — the sheet per spec; GET /cards/:id now carries sources-used/unavailable + day scratches for the footer |
| Card engine + decision trace (D10) | merged | PR #13 — lean allocation, all ticket rules, mandatory place-money sweep, exact bankroll, completeness, full trace to the decision-trace stream; conditions-text interleave fix in the program parser (goldens regenerated) |
| Consensus table + classification (D09) | merged | PR #12 — chips + contrarian flags on the day view; classification re-runs after every fetch and manual confirm; migration 004 (`races.contrarian_flags`) |
| SFTB fetcher (D08c) | merged | PR #9 — sitemap discovery + expected-order parsing; real fixture matches the program-PDF day. D08a CLOSED (dmtc picks page is a directory; covered by D05's Bottom Line extraction) and D08b CLOSED (ATR bot challenge; manual-paste only) — both by user decision 2026-09-01 |
| Backtesting addendum encoding (D25) | merged | PR #8, branch `backtest-addendum` — `cards.consensus_completeness` (migration 003), invariant 12 (buckets never pool), Wayback fetcher planned as D08d, signal/structure layer split planned into D18/D19. Completeness boundary user-confirmed 2026-09-01: FULL = every race ≥2 external sources |
| Program-PDF parser (D05) | merged | PR #5 — real Del Mar program (10 races, 98 entries, Bottom Line, index validation); found the printed-scratch/renumbered-index pattern in the wild |
